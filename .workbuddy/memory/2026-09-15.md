# 2026-09-15

## CodeAgent：完成 M3–M7（M0–M2 此前已完成并推送）

仓库 https://github.com/2229397189/Codeagent.git ，目录 D:\code\CodeAgent ，零依赖 Java 17。

| 模块 | 内容 | 提交 |
|------|------|------|
| M3 上下文治理 | ContextBudget（provider usage 记账 70/90/100 三档）、MicroCompact（确定性裁剪）、AutoCompact（达水位压缩+保留最近 user）、ToolResultStorage（大结果离屏）、DefaultContextGovernance（编排管道，M6 加） | b14219e |
| M4 权限 | DefaultPermissionManager：三态 ALLOW/DENY/ASK；路径沙箱、危险命令拦截（rm -rf/git reset --hard/sudo/fork bomb）、写前审批 + 持久化 permissions.json；权限门落在 ToolRegistry.execute | 902c05b |
| M5 会话 | SessionStore（JSONL append-only）+ Session（resume/rename/fork 隔离）；新增 Message.fromJsonLine / ToolCall.toJson | 3efea4c |
| M6 CLI | CodeAgentCli：REPL + /tools /status /compact /approve /session /help /exit；接线 loop+context+permission+session；**单回合错误隔离**（模型/网络异常不终结 REPL） | 752273b |
| M7 收尾 | docs/RESUME.md（bullet 逐条对代码 + 明确「没做」清单 + 面试追问表）、README 更新（REPL 命令、80 断言） | 155b922（本地，推送被 502 阻断） |

- 测试：**80 项断言全绿、0 失败**（AllTests 累加 6 个测试类）。原则是断言「返回值正确」而非仅不报错。
- 真实 GLM 调用：请求链路通、鉴权通过，但账号返回 `HTTP 429 余额不足/无可用资源包` → 真实模型跑不通，离线 mock 全通。
- 待办：M7 提交待 push（网络 502）；建议吊销已泄露的 GitHub PAT。

## 环境坑（本机 Git Bash shim，务必记住）

- 缺 coreutils：`cd` `rm` `find` `grep` `tail` `sleep` `env` `which` 均不可用（报 command not found 或 dirname/cd null directory）。
- 编译用：`javac -encoding UTF-8 -d <out> D:/.../src/main/java/com/codeagent/**/*.java`（globstar + 绝对路径，不要 cd）。
- 计数/过滤用：`findstr /C:"PASS" < file | /c/Windows/System32/find.exe /c /v ""`。
- PowerShell 工具本次不回显 stdout，改用 Bash。
- GitHub push 会间歇性 `CONNECT tunnel failed, response 502`，需循环重试；重试无效就先本地 commit，稍后再推。
